# LLM Server



## Setup

Clone the repository:

```bash
git clone https://github.com/dananvaro/llm-server.git
cd llm-server
```

Start the server:

```bash
docker compose up -d
```

Stop the server:

```bash
docker compose down
```
